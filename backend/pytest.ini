# Phase 2 Week 1 Day 6: Run only integration tests in tests/
# Root-level test_*.py files are manual/smoke scripts (expect seeded DB or live server).
[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v
