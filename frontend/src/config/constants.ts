/**
 * Single mode: one consistent UI for all users.
 * Try Demo, Login, Register, Plan, Timeline, Notifications, Build info, Reset demo (for demo user) — all available.
 */
export const isPortfolioMode = true;
