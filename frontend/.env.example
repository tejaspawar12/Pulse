# API base URL (no trailing slash). Used when running on physical device or emulator.
# For Expo WEB (Press w): app uses localhost automatically — you can leave this as-is.
#
# For PHYSICAL PHONE on same Wi-Fi as laptop:
# 1. On laptop, run: ipconfig
# 2. Find "Wireless LAN adapter Wi-Fi" → "IPv4 Address" (e.g. 192.168.1.5)
# 3. Set EXPO_PUBLIC_API_URL=http://THAT_IP:8000/api/v1
# 4. Allow port 8000 in Windows Firewall (Private networks) for Python/uvicorn
#
# Examples:
# EXPO_PUBLIC_API_URL=http://192.168.1.5:8000/api/v1
# EXPO_PUBLIC_API_URL=http://172.20.10.2:8000/api/v1
#
# Copy this file to .env and set your value:
EXPO_PUBLIC_API_URL=http://localhost:8000/api/v1

# Phase 3 — Portfolio mode (local refinement; no startup UI)
# When true: first screen = Try Demo + Login (no Register); Insights at top of History; no Verify Email / Plan / Timeline / Push / Accountability in UI
EXPO_PUBLIC_PORTFOLIO_MODE=false
